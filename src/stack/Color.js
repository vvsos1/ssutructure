
// 스택 아이템의 노드 색상
const stackItem = '#ff7f00';

// 스택 아이템의 텍스트 색상
const stackItemText = '#f5f6fa';

// 큐 아이템의 노드 색상
const queueItem = '#ff7f00';

// 큐 아이템의 텍스트 색상
const queueItemText = '#f5f6fa'

module.exports = {
    stackItem,
    stackItemText,

    queueItem,
    queueItemText
}